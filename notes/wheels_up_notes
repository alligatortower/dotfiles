# Wheels Up Notes

## user types
change field to userTypes

## card choosing WU
 - search (probably not)
 - categories
 - lock the height of the block chooser

## Page subnav
move data to object with items key with array
metadata into metadata object

## dismissable
container + card metadata for dismissable
pass that flag for containers

## text stuff
meta class flag for "contains template tag"


## things to do once adrian has done his work
- dismissables
- block group re-org. use master ticket classifications
